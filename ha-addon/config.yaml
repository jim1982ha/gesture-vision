# --- ha-addon/config.yaml --- (complete version) ---
name: "GestureVision"
version: "4.2.13"
slug: "ha-addon"
description: "AI-Powered Gesture Control using Webcams or RTSP cameras."
url: "https://github.com/jim1982ha/gesture-vision"
arch:
  - aarch64
  - amd64
startup: application
boot: auto
init: false
map:
  - "config:rw"
devices:
  - /dev/video0:rwm
  - /dev/video1:rwm
  - /dev/video2:rwm
ingress: true
ingress_port: 80
panel_icon: mdi:hand-wave

# HA Permissions Best Practices
# Enables access to the Core API (Control devices)
homeassistant_api: true
# Enables access to Supervisor API (Add-on info)
hassio_api: true
# Enables Ingress Authentication (User context)
auth_api: true

ports:
  8888/tcp: 8888
  8189/udp: 8189
  1935/tcp: 1935
  8554/tcp: 8554
options:
  mtx_ice_host: ""
  log_level: "info"
schema:
  mtx_ice_host: str?
  log_level: list(debug|info|warn|error)