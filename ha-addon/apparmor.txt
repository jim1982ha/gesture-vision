#include <tunables/global>

profile gesturevision flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  #include <abstractions/nameservice>
  #include <abstractions/user-tmp>

  # Network Access
  network inet stream,
  network inet6 stream,
  network inet dgram,
  network inet6 dgram,
  network raw,
  network packet,

  # Device Access (Webcams)
  /dev/video* rw,

  # File Access - App Structure
  # Granting Read, Write, Lock, Map, and Execute on directories and contents
  /app/ rwk,
  /app/** rwkmix,
  /data/ rwk,
  /data/** rwkmix,
  /config/ rwk,
  /config/** rwkmix,
  /run/ rwk,
  /run/** rwkmix,
  /tmp/ rwk,
  /tmp/** rwkmix,

  # System Configs & SSL (Required by Node.js 22+)
  /etc/ssl/** r,
  /etc/ca-certificates/** r,
  /usr/share/ca-certificates/** r,

  # Nginx specific
  /var/log/nginx/** rw,
  /var/lib/nginx/** rw,
  /etc/nginx/** r,
  /usr/share/nginx/html/ rwk,
  /usr/share/nginx/html/** rwkmix,

  # System Binaries required by Node/Scripts/Plugins
  /usr/local/bin/** ixr,
  /usr/local/lib/node_modules/** ixr,
  /{usr/,}bin/** ixr,
  /{usr/,}sbin/** ixr,

  # Shared Libraries
  /lib/** mr,
  /usr/lib/** mr,

  # Capabilities
  capability net_bind_service,
  capability chown,
  capability setgid,
  capability setuid,
  capability dac_override,
  capability sys_admin,

  # Process Management
  signal,
  ptrace,
}