#include <tunables/global>

profile gesturevision flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  #include <abstractions/nameservice>
  #include <abstractions/user-tmp>
  #include <abstractions/ssl_certs>

  # Network Access
  network inet stream,
  network inet6 stream,
  network inet dgram,
  network inet6 dgram,
  network raw,
  network packet,

  # Device Access (Webcams & essential IO)
  /dev/video* rw,
  /dev/tty rw,
  /dev/null rw,
  /dev/urandom r,
  /dev/random r,

  # System Info & Metrics (Vital for Node.js performance monitors)
  @{PROC}/** r,
  /sys/** r,
  /etc/timezone r,
  /etc/localtime r,

  # File Access - App Structure
  # Granting Read, Write, Lock, Map, and Execute on directories and contents
  /app/ rwk,
  /app/** rwkmix,
  /data/ rwk,
  /data/** rwkmix,
  /config/ rwk,
  /config/** rwkmix,
  /run/ rwk,
  /run/** rwkmix,
  /tmp/ rwk,
  /tmp/** rwkmix,
  /var/run/** rwkmix,

  # System Configs, SSL, and Perl
  /etc/ssl/** r,
  /etc/ca-certificates/** r,
  /etc/perl/** r,

  # Nginx specific
  /var/log/nginx/** rw,
  /var/lib/nginx/** rw,
  /etc/nginx/** rwkmix,
  /usr/share/** r,
  /usr/share/nginx/** rwkmix,

  # System Binaries & Shared Libs (Multi-arch support)
  /usr/local/bin/** ixr,
  /usr/local/lib/node_modules/** ixr,
  /{usr/,}bin/** ixr,
  /{usr/,}sbin/** ixr,
  /lib/** mr,
  /usr/lib/** mr,
  /usr/lib/x86_64-linux-gnu/** mr,
  /usr/lib/aarch64-linux-gnu/** mr,
  /usr/lib/arm-linux-gnueabihf/** mr,

  # Capabilities
  capability net_bind_service,
  capability chown,
  capability setgid,
  capability setuid,
  capability dac_override,
  capability sys_admin,

  # Process Management
  signal,
  ptrace,
}