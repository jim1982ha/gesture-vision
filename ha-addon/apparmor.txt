#include <tunables/global>

profile gesturevision flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  #include <abstractions/nameservice>
  #include <abstractions/user-tmp>

  # Network Access
  network inet stream,
  network inet6 stream,
  network inet dgram,
  network inet6 dgram,
  network raw,
  network packet,

  # Device Access (Webcams)
  /dev/video* rw,

  # File Access - App Structure
  /app/** r,
  /app/** ix,
  /app/** m,
  /data/** rwk,
  /config/** rwk,
  /run/** rw,
  /tmp/** rwk,

  # Nginx specific
  /var/log/nginx/** rw,
  /var/lib/nginx/** rw,
  /etc/nginx/** r,

  # System Binaries required by Node/Scripts/Plugins
  /usr/local/bin/mediamtx ixr,
  /usr/local/bin/docker-entrypoint.sh ixr,
  /usr/sbin/nginx ixr,
  /usr/local/bin/node ixr,
  /usr/local/bin/npm ixr,
  /usr/bin/git ixr,
  /usr/bin/curl ixr,
  /usr/bin/jq ixr,
  /usr/bin/rsync ixr,
  /usr/bin/env ixr,
  /{usr/,}bin/dash ixr,
  /{usr/,}bin/bash ixr,
  /{usr/,}bin/sh ixr,
  /{usr/,}bin/mkdir ixr,
  /{usr/,}bin/cp ixr,
  /{usr/,}bin/rm ixr,
  /{usr/,}bin/ln ixr,
  /{usr/,}bin/ls ixr,
  /{usr/,}bin/cat ixr,
  /{usr/,}bin/chmod ixr,
  /{usr/,}bin/grep ixr,
  /{usr/,}bin/sed ixr,
  /{usr/,}bin/awk ixr,

  # Shared Libraries
  /lib/** mr,
  /usr/lib/** mr,

  # Capabilities
  capability net_bind_service,
  capability chown,
  capability setgid,
  capability setuid,
  capability dac_override,
  capability sys_admin,

  # Process Management
  signal,
  ptrace,
}